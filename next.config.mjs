/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // Set the basePath if your GitHub Pages site is hosted under a subpath
    // Replace 'my-nextjs-site' with the name of your GitHub repository
    basePath: '/studyHub',
  };
  
  export default nextConfig;
  